{# src/Phil/UserBundle/Resources/views/User/update.html.twig #}
{% extends 'PhilUserBundle::layout.html.twig' %}

{% block title %}philmowatt.com - update profile{% endblock %}

{% block content %}
    <h2>Update Profile</h2>
    {% for flashMessage in app.session.flashbag.get('notice') %}
        <div class="flash-notice">
            {{ flashMessage }}
        </div>
    {% endfor %}
    <a href="{{ path('user_changepassword') }}">Change Password</a>
    {{ form(form) }}
{% endblock %}
