{# src/Phil/UserBundle/Resources/views/User/resendActivation.html.twig #}
{% extends 'PhilUserBundle::layout.html.twig' %}

{% block title %}philmowatt.com - {{ title|lower }}{% endblock %}

{% block content %}
    <h2>{{ title }}</h2>
    {% for flashMessage in app.session.flashbag.get('notice') %}
        <div class="flash-notice">
            {{ flashMessage }}
        </div>
    {% endfor %}
    {{ form(form) }}
{% endblock %}
